package Sistema.service;

public class PessoaFisicaService {
    public double adicionaSalario(double salario) {
        if (salario <=) throw new IllegalArgumentException("o salario nao pode ser menor ou igual a zero");
        if (Double.isNaN(salario)) throw new
        return salario;
    }
}
